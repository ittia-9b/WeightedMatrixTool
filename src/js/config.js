const config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                primary: {
                    DEFAULT: '#0000FF',
                    hover: '#0056b3'
                },
                danger: '#FF453A',
                success: '#32D74B',
            }
        }
    }
};

export default config; 