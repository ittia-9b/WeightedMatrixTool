const config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                primary: {
                    DEFAULT: '#007AFF',
                    hover: '#0056b3'
                },
                danger: '#FF453A',
                success: '#32D74B',
            }
        }
    }
};

export default config; 